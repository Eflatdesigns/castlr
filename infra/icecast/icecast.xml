<?xml version="1.0"?>
<!--
  Castlr — Icecast2 configuration
  Install: apt install icecast2
  Config:  /etc/icecast2/icecast.xml

  Replace CHANGEME values before deploying.
  Ports: 8000 (HTTP stream), 8443 (HTTPS if SSL termination via nginx)
-->
<icecast>
  <location>Earth</location>
  <admin>castlr@yourdomain.com</admin>
  <limits>
    <clients>500</clients>
    <sources>50</sources>
    <queue-size>524288</queue-size>
    <client-timeout>30</client-timeout>
    <header-timeout>15</header-timeout>
    <source-timeout>10</source-timeout>
    <burst-on-connect>1</burst-on-connect>
    <burst-size>65535</burst-size>
  </limits>

  <authentication>
    <source-password>ICECAST_SOURCE_PASSWORD</source-password>
    <relay-password>ICECAST_RELAY_PASSWORD</relay-password>
    <admin-user>admin</admin-user>
    <admin-password>ICECAST_ADMIN_PASSWORD</admin-password>
  </authentication>

  <hostname>your-server.com</hostname>

  <listen-socket>
    <port>8000</port>
    <bind-address>0.0.0.0</bind-address>
  </listen-socket>

  <!-- Allow cross-origin requests from the Castlr frontend -->
  <http-headers>
    <header name="Access-Control-Allow-Origin" value="https://yourdomain.com" />
    <header name="Access-Control-Allow-Headers" value="Origin, Accept, X-Requested-With, Content-Type" />
    <header name="Access-Control-Allow-Methods" value="GET, OPTIONS, HEAD" />
  </http-headers>

  <paths>
    <basedir>/usr/share/icecast2</basedir>
    <logdir>/var/log/icecast2</logdir>
    <webroot>/usr/share/icecast2/web</webroot>
    <adminroot>/usr/share/icecast2/admin</adminroot>
    <pidfile>/run/icecast2/icecast2.pid</pidfile>
    <alias source="/" destination="/status.xsl"/>
  </paths>

  <logging>
    <accesslog>access.log</accesslog>
    <errorlog>error.log</errorlog>
    <loglevel>3</loglevel>
    <logsize>10000</logsize>
  </logging>

  <security>
    <chroot>0</chroot>
  </security>
</icecast>
