[Unit]
Description=Liquidsoap Audio Processing
After=icecast2.service network.target
Requires=icecast2.service

[Service]
Type=simple
User=liquidsoap
ExecStart=/usr/bin/liquidsoap /etc/liquidsoap/castlr.liq
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
